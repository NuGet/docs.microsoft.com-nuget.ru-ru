---
title: "Переустановка и обновление пакетов NuGet | Документы Майкрософт"
author: kraigb
ms.author: kraigb
manager: ghogen
ms.date: 12/07/2017
ms.topic: article
ms.prod: nuget
ms.technology: 
ms.assetid: 2785879b-97f0-4a85-b3cc-bf4eaa5c39bf
description: "Сведения о том, когда нужно переустанавливать и обновлять пакеты, например при нарушенных ссылках на пакеты в Visual Studio."
keywords: "установка пакета NuGet, переустановка пакета NuGet, восстановление пакета NuGet, обновление пакета, восстановление пакетов, исправление нарушенных ссылок"
ms.reviewer:
- karann-msft
- unniravindranathan
ms.openlocfilehash: 6a198b371c86166e2bcdee7f6cf2a6c971bea0a3
ms.sourcegitcommit: bdcd2046b1b187d8b59716b9571142c02181c8fb
ms.translationtype: HT
ms.contentlocale: ru-RU
ms.lasthandoff: 01/10/2018
---
# <a name="how-to-reinstall-and-update-packages"></a><span data-ttu-id="e8dd4-104">Переустановка и обновление пакетов</span><span class="sxs-lookup"><span data-stu-id="e8dd4-104">How to reinstall and update packages</span></span>

<span data-ttu-id="e8dd4-105">Существует несколько ситуаций, описанных ниже в разделе [Когда следует переустанавливать пакет](#when-to-reinstall-a-package), когда ссылки на пакет в проекте Visual Studio могут быть нарушены.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-105">There are a number of situations, described below under [When to Reinstall a Package](#when-to-reinstall-a-package), where references to a package might get broken within a Visual Studio project.</span></span> <span data-ttu-id="e8dd4-106">В этих случаях удаление и повторная установка той же версии пакета восстанавливает работоспособность ссылок.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-106">In these cases, uninstalling and then reinstalling the same version of the package will restore those references to working order.</span></span> <span data-ttu-id="e8dd4-107">Обновление пакета означает простую установку обновленной версии, что часто позволяет восстановить работоспособность пакета.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-107">Updating a package simply means installing an updated version, which often restores a package to working order.</span></span>

<span data-ttu-id="e8dd4-108">Обновление и переустановка пакетов выполняются следующим образом:</span><span class="sxs-lookup"><span data-stu-id="e8dd4-108">Updating and reinstalling packages is accomplished as follows:</span></span>

| <span data-ttu-id="e8dd4-109">Метод</span><span class="sxs-lookup"><span data-stu-id="e8dd4-109">Method</span></span> | <span data-ttu-id="e8dd4-110">Обновление</span><span class="sxs-lookup"><span data-stu-id="e8dd4-110">Update</span></span> | <span data-ttu-id="e8dd4-111">Переустановка</span><span class="sxs-lookup"><span data-stu-id="e8dd4-111">Reinstall</span></span> | 
| --- | --- | --- |
| <span data-ttu-id="e8dd4-112">Консоль диспетчера пакетов (описана в разделе [Использование команды Update-Package](#using-update-package))</span><span class="sxs-lookup"><span data-stu-id="e8dd4-112">Package Manager console (described in [Using Update-Package](#using-update-package))</span></span> | <span data-ttu-id="e8dd4-113">Команда `Update-Package`</span><span class="sxs-lookup"><span data-stu-id="e8dd4-113">`Update-Package` command</span></span> | <span data-ttu-id="e8dd4-114">Команда `Update-Package -reinstall`</span><span class="sxs-lookup"><span data-stu-id="e8dd4-114">`Update-Package -reinstall` command</span></span> |
| <span data-ttu-id="e8dd4-115">Пользовательский интерфейс диспетчера пакетов</span><span class="sxs-lookup"><span data-stu-id="e8dd4-115">Package Manager UI</span></span> | <span data-ttu-id="e8dd4-116">На вкладке **Обновления** выберите один или несколько пакетов и затем элемент **Обновить**</span><span class="sxs-lookup"><span data-stu-id="e8dd4-116">On the **Updates** tab, select one or more packages and select **Update**</span></span> | <span data-ttu-id="e8dd4-117">На вкладке **Установленные** выберите пакет, запишите его имя, а затем выберите элемент **Удалить**.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-117">On the **Installed** tab, select a package, record its name, then select **Uninstall**.</span></span> <span data-ttu-id="e8dd4-118">Перейдите на вкладку **Обзор**, найдите имя пакета, выберите его, а затем выберите элемент **Установить**.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-118">Switch to the **Browse** tab, search for the package name, select it, then select **Install**).</span></span> |
| <span data-ttu-id="e8dd4-119">Интерфейс командной строки nuget.exe</span><span class="sxs-lookup"><span data-stu-id="e8dd4-119">nuget.exe CLI</span></span> | <span data-ttu-id="e8dd4-120">Команда `nuget update`</span><span class="sxs-lookup"><span data-stu-id="e8dd4-120">`nuget update` command</span></span> | <span data-ttu-id="e8dd4-121">Для всех пакетов удалите папку пакета, а затем запустите `nuget install`.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-121">For all packages, delete the package folder, then run `nuget install`.</span></span> <span data-ttu-id="e8dd4-122">Для одного пакета удалите папку пакета, а затем используйте `nuget install <id>` для переустановки того же пакета.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-122">For a single package, delete the package folder and use `nuget install <id>` to reinstall the same one.</span></span> |

<span data-ttu-id="e8dd4-123">Содержание этой статьи</span><span class="sxs-lookup"><span data-stu-id="e8dd4-123">In this article:</span></span>

- [<span data-ttu-id="e8dd4-124">Когда следует переустанавливать пакет</span><span class="sxs-lookup"><span data-stu-id="e8dd4-124">When to Reinstall a Package</span></span>](#when-to-reinstall-a-package)
- [<span data-ttu-id="e8dd4-125">Ограничение версий для обновления</span><span class="sxs-lookup"><span data-stu-id="e8dd4-125">Constraining upgrade versions</span></span>](#constraining-upgrade-versions)

## <a name="when-to-reinstall-a-package"></a><span data-ttu-id="e8dd4-126">Когда следует переустанавливать пакет</span><span class="sxs-lookup"><span data-stu-id="e8dd4-126">When to Reinstall a Package</span></span>

1. <span data-ttu-id="e8dd4-127">**Нарушенные ссылки после восстановления пакета**: если вы открыли проект и восстановили пакеты NuGet, но по-прежнему видите нарушенные ссылки, попробуйте переустановить каждый из этих пакетов.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-127">**Broken references after package restore**: If you've opened a project and restored NuGet packages, but still see broken references, try reinstalling each of those packages.</span></span>
1. <span data-ttu-id="e8dd4-128">**Проект нарушен из-за удаленных файлов**: NuGet не запрещает вам удалять элементы, добавленные из пакетов, поэтому вы легко можете случайно изменить содержимое, установленное из пакета, и нарушить работу проекта.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-128">**Project is broken due to deleted files**: NuGet does not prevent you from removing items added from packages, so it's easy to inadvertently modify contents installed from a package and break your project.</span></span> <span data-ttu-id="e8dd4-129">Чтобы восстановить проект, переустановите затронутые пакеты.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-129">To restore the project, reinstall the affected packages.</span></span>
1. <span data-ttu-id="e8dd4-130">**Обновление пакета нарушило работу проекта**: если обновление пакета нарушает работу проекта, причиной сбоя обычно является пакет зависимостей, который также мог быть обновлен.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-130">**Package update broke the project**: If an update to a package breaks a project, the failure is generally caused by a dependency package which may have also been updates.</span></span> <span data-ttu-id="e8dd4-131">Чтобы восстановить состояние зависимости, переустановите этот пакет.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-131">To restore the state of the dependency, reinstall that specific package.</span></span>
1. <span data-ttu-id="e8dd4-132">**Перенацеливание или обновление проекта**: это может быть полезно, когда проект был перенацелен или обновлен и если он требует переустановки из-за изменения целевой платформы.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-132">**Project retargeting or upgrade**: This can be useful when a project has been retargeted or upgraded and if the package requires reinstallation due to the change in target framework.</span></span> <span data-ttu-id="e8dd4-133">В таких случаях NuGet 2.7 и более поздних версий выдает ошибку сборки сразу после перенацеливания проекта, а последующие предупреждения сборки позволяют понять, что для пакета может потребоваться переустановка.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-133">NuGet 2.7 and later shows a build error in such cases immediately after project retargeting, and subsequent build warnings let you know that the package may need to be reinstalled.</span></span> <span data-ttu-id="e8dd4-134">Для обновления проекта NuGet выводит ошибку в журнале обновления проекта.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-134">For project upgrade, NuGet shows an error in the Project Upgrade Log.</span></span>
1. <span data-ttu-id="e8dd4-135">**Переустановка пакета во время его разработки**: авторам пакетов часто требуется переустановить ту версию пакета, которую они разрабатывают, для тестирования поведения.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-135">**Reinstalling a package during its development**: Package authors often need to reinstall the same version of package they're developing to test the behavior.</span></span> <span data-ttu-id="e8dd4-136">Команда `Install-Package` не предоставляет возможность принудительной переустановки, поэтому используйте вместо нее `Update-Package -reinstall`.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-136">The `Install-Package` command does not provide an option to force a reinstall, so use `Update-Package -reinstall` instead.</span></span>

## <a name="constraining-upgrade-versions"></a><span data-ttu-id="e8dd4-137">Ограничение версий для обновления</span><span class="sxs-lookup"><span data-stu-id="e8dd4-137">Constraining upgrade versions</span></span>

<span data-ttu-id="e8dd4-138">По умолчанию для переустановки или обновлении пакета *всегда* устанавливается самая последняя доступная версия из источника пакета.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-138">By default, reinstalling or updating a package *always* installs the latest version available from the package source.</span></span>

<span data-ttu-id="e8dd4-139">Однако в проектах, использующих формат ссылок `packages.config`, вы можете ограничить диапазон версий.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-139">In projects using the `packages.config` reference format, however, you can specifically constrain the version range.</span></span> <span data-ttu-id="e8dd4-140">Например, если известно, что приложение работает с версией 1.x пакета, но не работает с версией 2.0 и выше, возможно, из-за значительного изменения в API пакета, может потребоваться ограничить обновление версиями 1.x.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-140">For example, if you know that your application works only with version 1.x of a package but not 2.0 and above, perhaps due to a major change in the package API, then you'd want to constrain upgrades to 1.x versions.</span></span> <span data-ttu-id="e8dd4-141">Это предотвратит случайные обновления, приводящие к сбою приложения.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-141">This prevents accidental updates that would break the application.</span></span>

<span data-ttu-id="e8dd4-142">Чтобы задать ограничение, откройте `packages.config` в текстовом редакторе, найдите требуемую зависимость и добавьте атрибут `allowedVersions` с диапазоном версий.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-142">To set a constraint, open `packages.config` in a text editor, locate the dependency in question, and add the `allowedVersions` attribute with a version range.</span></span> <span data-ttu-id="e8dd4-143">Например, чтобы ограничить обновления версией 1.x, задайте для `allowedVersions` значение `[1,2)`:</span><span class="sxs-lookup"><span data-stu-id="e8dd4-143">For example, to constrain updates to version 1.x, set `allowedVersions` to `[1,2)`:</span></span>

```xml
<?xml version="1.0" encoding="utf-8"?>
<packages>
    <package id="ExamplePackage" version="1.1.0" allowedVersions="[1,2)" />

    <!-- ... -->
</packages>
```

<span data-ttu-id="e8dd4-144">Во всех случаях используйте нотацию, описанную в статье [Управление версиями пакета](../reference/package-versioning.md#version-ranges-and-wildcards).</span><span class="sxs-lookup"><span data-stu-id="e8dd4-144">In all cases, use the notation described in [Package versioning](../reference/package-versioning.md#version-ranges-and-wildcards).</span></span>

## <a name="using-update-package"></a><span data-ttu-id="e8dd4-145">Использование команды Update-Package</span><span class="sxs-lookup"><span data-stu-id="e8dd4-145">Using Update-Package</span></span>

<span data-ttu-id="e8dd4-146">Учитывая описанные ниже [особенности](#considerations), вы можете легко переустановить пакет с помощью [команды Update-Package](../Tools/ps-ref-update-package.md) в консоли диспетчера пакетов Visual Studio (**Сервис** > **Диспетчер пакетов NuGet** > **Консоль диспетчера пакетов**):</span><span class="sxs-lookup"><span data-stu-id="e8dd4-146">Being mindful of the [Considerations](#considerations) described below, you can easily reinstall any package using the [Update-Package command](../Tools/ps-ref-update-package.md) in the Visual Studio Package Manager Console (**Tools** > **NuGet Package Manager** > **Package Manager Console**):</span></span>

```ps
Update-Package -Id <package_name> –reinstall
```

<span data-ttu-id="e8dd4-147">Воспользоваться этой командой гораздо проще, чем удалить пакет и затем пытаться найти в коллекции NuGet пакет той же версии.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-147">Using this command is much easier than removing a package and then trying to locate the same package in the NuGet gallery with the same version.</span></span> <span data-ttu-id="e8dd4-148">Обратите внимание, что параметр `-Id` является необязательным.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-148">Note that the `-Id` switch is optional.</span></span>

<span data-ttu-id="e8dd4-149">Та же команда без `-reinstall` обновляет пакет до более новой версии, если это возможно.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-149">The same command without `-reinstall` updates a package to a newer version, if applicable.</span></span> <span data-ttu-id="e8dd4-150">Команда `Update-Package` выдает ошибку, если соответствующий пакет не установлен в проекте, то есть эта команда не устанавливает сами пакеты.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-150">The command gives an error if the package in question is not already installed in a project; that is, `Update-Package` does not install packages directly.</span></span>

```ps
Update-Package <package_name>
```

<span data-ttu-id="e8dd4-151">По умолчанию `Update-Package` затрагивает все пакеты в решении.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-151">By default, `Update-Package` affects all packages in a solution.</span></span> <span data-ttu-id="e8dd4-152">Чтобы ограничить действие конкретным проектом, используйте параметр `-ProjectName`, указав имя проекта в том виде, в котором оно отображается в обозревателе решений:</span><span class="sxs-lookup"><span data-stu-id="e8dd4-152">To limit the action to a specific project, use the `-ProjectName` switch, using the name of the project as it appears in Solution Explorer:</span></span>

```ps
# Reinstall the package in just MyProject
Update-Package <package_name> -ProjectName MyProject -reinstall
```
<span data-ttu-id="e8dd4-153">Чтобы *обновить* все пакеты в проекте (или переустановить с помощью `-reinstall`), используйте `-ProjectName` без указания конкретного пакета:</span><span class="sxs-lookup"><span data-stu-id="e8dd4-153">To *update* all packages in a project (or reinstall using `-reinstall`), use `-ProjectName` without specifying any particular package:</span></span>

```ps
Update-Package -ProjectName MyProject
```

<span data-ttu-id="e8dd4-154">Чтобы обновить все пакеты в решении, используйте только команду `Update-Package` без аргументов или параметров.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-154">To update all packages in a solution, just use `Update-Package` by itself with no other arguments or switches.</span></span> <span data-ttu-id="e8dd4-155">Используйте эту форму осторожно, так как выполнение всех обновлений может занимать много времени:</span><span class="sxs-lookup"><span data-stu-id="e8dd4-155">Use this form carefully, because it can take considerable time to perform all the updates:</span></span>

```ps
# Updates all packages in all projects in the solution
Update-Package 
```

<span data-ttu-id="e8dd4-156">При обновлении пакетов в проекте или решении с помощью `project.json` или [ссылок на пакеты в файлах проектов](../Consume-Packages/Package-References-in-Project-Files.md) всегда применяется последняя версия пакета (кроме пакетов предварительного выпуска).</span><span class="sxs-lookup"><span data-stu-id="e8dd4-156">Updating packages in a project or solution using `project.json` or [package references in project files](../Consume-Packages/Package-References-in-Project-Files.md) always updates to the latest version of the package (excluding pre-release packages).</span></span> <span data-ttu-id="e8dd4-157">Проекты, использующие `packages.config`, при необходимости могут ограничить версии обновления, как описано ниже в разделе [Ограничение версий для обновления](#constraining-upgrade-versions).</span><span class="sxs-lookup"><span data-stu-id="e8dd4-157">Projects that use `packages.config` can, if desired, limit update versions as described below in [Constraining upgrade versions](#constraining-upgrade-versions).</span></span>

<span data-ttu-id="e8dd4-158">Дополнительные сведения о команде см. по ссылке [Update-Package](../Tools/ps-ref-update-package.md).</span><span class="sxs-lookup"><span data-stu-id="e8dd4-158">For full details on the command, see the [Update-Package](../Tools/ps-ref-update-package.md) reference.</span></span>

### <a name="considerations"></a><span data-ttu-id="e8dd4-159">Особенности</span><span class="sxs-lookup"><span data-stu-id="e8dd4-159">Considerations</span></span>

<span data-ttu-id="e8dd4-160">Переустановка пакета может повлиять на следующее:</span><span class="sxs-lookup"><span data-stu-id="e8dd4-160">The following may be affected when reinstalling a package:</span></span>

1. <span data-ttu-id="e8dd4-161">**Переустановка пакетов в результате перенацеливания целевой платформы проекта**</span><span class="sxs-lookup"><span data-stu-id="e8dd4-161">**Reinstalling packages according to project target framework retargeting**</span></span>
    - <span data-ttu-id="e8dd4-162">В простом случае можно выполнить переустановку пакета с помощью `Update-Package –reinstall <package_name>`.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-162">In a simple case, just reinstalling a package using `Update-Package –reinstall <package_name>` works.</span></span> <span data-ttu-id="e8dd4-163">Пакет, установленный для старой целевой платформы, удаляется, после чего устанавливается тот же самый пакет для текущей целевой версии проекта.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-163">A package that is installed against an old target framework gets uninstalled and the same package gets installed against the current target framework of the project.</span></span>
    - <span data-ttu-id="e8dd4-164">В некоторых случаях пакет может не поддерживать новую целевую платформу.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-164">In some cases, there may be a package that does not support the new target framework.</span></span>
        - <span data-ttu-id="e8dd4-165">Если пакет поддерживает переносимые библиотеки классов (PCL), а проект перенацеливается на сочетание платформ, которое больше не поддерживается пакетом, после переустановки ссылки на этот пакет будут отсутствовать.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-165">If a package supports portable class libraries (PCLs) and the project is retargeted to a combination of platforms no longer supported by the package, references to the package will be missing after reinstalling.</span></span>
        - <span data-ttu-id="e8dd4-166">Это может касаться пакетов, которые вы используете напрямую, или пакетов, установленных в качестве зависимостей.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-166">This can surface for packages you're using directly or for packages installed as dependencies.</span></span> <span data-ttu-id="e8dd4-167">Используемый напрямую пакет может поддерживать новую целевую платформу, а его зависимость — нет.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-167">It's possible for the package you're using directly to support the new target framework while its dependency does not.</span></span>
        - <span data-ttu-id="e8dd4-168">Если переустановка пакетов после перенацеливания приложения приводит к ошибкам сборки или времени выполнения, может потребоваться отменить изменение целевой платформы или найти альтернативные пакеты, должным образом поддерживающие новую целевую платформу.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-168">If reinstalling packages after retargeting your application results in build or runtime errors, you may need to revert your target framework or search for alternative packages that properly support your new target framework.</span></span>

1. <span data-ttu-id="e8dd4-169">**Добавление атрибута requireReinstallation в файл packages.config после обновления или перенацеливания проекта**</span><span class="sxs-lookup"><span data-stu-id="e8dd4-169">**requireReinstallation attribute added in packages.config after project retargeting or upgrade**</span></span>
    - <span data-ttu-id="e8dd4-170">Если NuGet обнаруживает, что пакеты были затронуты при перенацеливании или обновлении проекта, он добавляет атрибут `requireReinstallation="true"` в `packages.config` для всех затронутых ссылок на пакет.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-170">If NuGet detects that packages were affected by retargeting or upgrading a project, it adds a `requireReinstallation="true"` attribute in  `packages.config` to all affected package references.</span></span> <span data-ttu-id="e8dd4-171">По этой причине каждая последующая сборка в Visual Studio вызывает предупреждения сборки для пакетов, чтобы вы не забыли переустановить их.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-171">Because of this, each subsequent build in Visual Studio raises build warnings for those packages so you can remember to reinstall them.</span></span>

1. <span data-ttu-id="e8dd4-172">**Переустановка пакетов с зависимостями**</span><span class="sxs-lookup"><span data-stu-id="e8dd4-172">**Reinstalling packages with dependencies**</span></span>
    - <span data-ttu-id="e8dd4-173">`Update-Package –reinstall` переустанавливает ту же версию исходного пакета, но устанавливает последнюю версию зависимостей, если только не заданы определенные ограничения по версии.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-173">`Update-Package –reinstall` reinstalls the same version of the original package, but installs the latest version of dependencies unless specific version constraints are provided.</span></span> <span data-ttu-id="e8dd4-174">Это позволяет обновить только зависимости, чтобы устранить ошибку.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-174">This allows you to update only the dependencies as required to fix an issue.</span></span> <span data-ttu-id="e8dd4-175">Однако, если при этом зависимость откатывается до более ранней версии, можно использовать `Update-Package <dependency_name>`, чтобы переустановить эту отдельную зависимость, не затрагивая зависимый пакет.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-175">However, if this rolls a dependency back to an earlier version, you can use `Update-Package <dependency_name>` to reinstall that one dependency without affecting the dependent package.</span></span>
    - <span data-ttu-id="e8dd4-176">`Update-Package –reinstall <packageName> -ignoreDependencies` переустанавливает ту же версию исходного пакета, но не переустанавливает зависимости.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-176">`Update-Package –reinstall <packageName> -ignoreDependencies` reinstalls the same version of the original package but does not reinstall dependencies.</span></span> <span data-ttu-id="e8dd4-177">Используйте эту команду, когда обновление зависимостей пакетов может привести к нарушению состояния.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-177">Use this when updating package dependencies might result in a broken state</span></span>

1. <span data-ttu-id="e8dd4-178">**Переустановка пакетов при использовании зависимых версий**</span><span class="sxs-lookup"><span data-stu-id="e8dd4-178">**Reinstalling packages when dependent versions are involved**</span></span>
    - <span data-ttu-id="e8dd4-179">Как описано выше, переустановка пакета не приводит к изменению версий других установленных пакетов, зависящих от него.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-179">As explained above, reinstalling a package does not change versions of any other installed packages that depend on it.</span></span> <span data-ttu-id="e8dd4-180">Поэтому возможно, что переустановка зависимости может привести к нарушению зависимого пакета.</span><span class="sxs-lookup"><span data-stu-id="e8dd4-180">It's possible, then, that reinstalling a dependency could break the dependent package.</span></span>

